# Energy-Demand-Prediction-API
A FastAPI application that serves machine learning models (LightGBM/XGBoost) for real‑time energy demand prediction. The API provides /predict and /train endpoints, processes JSON requests, and returns demand forecasts in kWh. Designed for deployment on Railway with Python dependencies managed via requirements.txt and startup defined in Procfile.
